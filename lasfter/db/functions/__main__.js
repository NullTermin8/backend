/**
* A basic Hello World function
* @param {string} email Who you're saying hello to
* @param {string} password Who you're saying hello to
* @returns {string}
*/
module.exports = (email, password, context, callback) => {
  return callback("bad route");
};
